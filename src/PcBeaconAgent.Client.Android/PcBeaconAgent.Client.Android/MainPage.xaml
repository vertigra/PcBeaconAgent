<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:viewModels="clr-namespace:PcBeaconAgent.Client.Android.ViewModels"
             xmlns:models="clr-namespace:PcBeaconAgent.Client.Core.Models;assembly=PcBeaconAgent.Client.Core"
             x:Class="PcBeaconAgent.Client.Android.MainPage"
             x:DataType="viewModels:MainViewModel"
             BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#121212}"
             Title="Agents Discovery">

    <ContentPage.Resources>
        <ResourceDictionary>
            <toolkit:InvertedBoolConverter x:Key="InvertedBoolConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto, *" Padding="16">

        <Border Grid.Row="0" 
                StrokeShape="RoundRectangle 12" 
                Stroke="#333333" 
                StrokeThickness="1"
                BackgroundColor="#1E1E1E" 
                Padding="15">
            <HorizontalStackLayout Spacing="12" VerticalOptions="Center">
                <Button Text="Start Scan" 
                        Command="{Binding StartScanCommand}" 
                        IsEnabled="{Binding IsScanning, Converter={StaticResource InvertedBoolConverter}}"
                        BackgroundColor="DarkSlateBlue" TextColor="White" CornerRadius="8" />

                <Button Text="Stop Scan" 
                        Command="{Binding StopScanCommand}" 
                        IsEnabled="{Binding IsScanning}"
                        BackgroundColor="IndianRed" TextColor="White" CornerRadius="8" />

                <ActivityIndicator IsRunning="{Binding IsScanning}" Color="DarkSlateBlue" VerticalOptions="Center" WidthRequest="30" HeightRequest="30" Margin="12,0,0,0"/>
            </HorizontalStackLayout>
        </Border>

        <CollectionView Grid.Row="1" ItemsSource="{Binding Devices}" Margin="0,16,0,0">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:BeaconDevice">
                    <Border StrokeShape="RoundRectangle 12" 
                            Stroke="#333333" 
                            StrokeThickness="1"
                            BackgroundColor="#1E1E1E" 
                            Padding="15"
                            Margin="0,0,0,10">

                        <Grid ColumnDefinitions="*, Auto">

                            <VerticalStackLayout Grid.Column="0" Spacing="4">
                                <Label Text="{Binding MachineName}" FontSize="18" FontAttributes="Bold" />
                                <Label Text="{Binding IpAddress, StringFormat='IP: {0}'}" FontSize="14" TextColor="DimGray" />
                                <Label Text="{Binding LastSeen, StringFormat='Last Response: {0:HH:mm:ss}'}" FontSize="11" TextColor="DarkGray" />
                            </VerticalStackLayout>

                            <HorizontalStackLayout Grid.Column="1" Spacing="6" VerticalOptions="Center">

                                <BoxView WidthRequest="12" HeightRequest="12" CornerRadius="6" 
                                         Color="Gray" 
                                         VerticalOptions="Center">
                                    <BoxView.Triggers>
                                        <DataTrigger TargetType="BoxView" Binding="{Binding Status}" Value="Online">
                                            <Setter Property="Color" Value="Green" />
                                        </DataTrigger>
                                    </BoxView.Triggers>
                                </BoxView>

                                <Label Text="{Binding Status}" FontAttributes="Bold" FontSize="14"
                                       TextColor="Gray" 
                                       VerticalOptions="Center">
                                    <Label.Triggers>
                                        <DataTrigger TargetType="Label" Binding="{Binding Status}" Value="Online">
                                            <Setter Property="TextColor" Value="Green" />
                                        </DataTrigger>
                                    </Label.Triggers>
                                </Label>

                            </HorizontalStackLayout>

                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>