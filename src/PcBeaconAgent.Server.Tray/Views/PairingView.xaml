<UserControl x:Class="PcBeaconAgent.Server.Tray.Views.PairingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PcBeaconAgent.Server.Tray.ViewModels"
             xmlns:local="clr-namespace:PcBeaconAgent.Server.Tray.Converters"
             Background="#1E1E1E">
    <UserControl.Resources>
        <local:InvertBoolConverter x:Key="InvertBoolConverter"/>
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Pairing PIN"
                   FontSize="13" Foreground="#9E9E9E"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1"
                   Text="{Binding CurrentPin}"
                   FontSize="42" FontWeight="Bold" Foreground="White"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontFamily="Consolas"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Center" Margin="0,12,0,0">
            <Button Content="Regenerate PIN"
                    Command="{Binding RegeneratePinCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InvertBoolConverter}}"
                    Padding="20,10"
                    Background="#6750A4" Foreground="White"
                    FontWeight="Medium"/>
        </StackPanel>
    </Grid>
</UserControl>
