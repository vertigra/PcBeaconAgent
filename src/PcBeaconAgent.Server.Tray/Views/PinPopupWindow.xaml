<Window x:Class="PcBeaconAgent.Server.Tray.Views.PinPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PcBeaconAgent PIN"
        Width="300" Height="200"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Topmost="True"
        ShowActivated="False"
        WindowStartupLocation="Manual">
    <Border Background="#1E1E1E"
            CornerRadius="10"
            BorderBrush="#3A3A3A"
            BorderThickness="1"
            Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="Pairing PIN"
                           FontSize="13"
                           Foreground="#9E9E9E"/>
                <Button Grid.Column="1"
                        x:Name="CloseButton"
                        Content="×"
                        Width="24" Height="24"
                        Background="Transparent"
                        Foreground="#9E9E9E"
                        BorderThickness="0"
                        FontSize="18"
                        FontWeight="Bold"
                        Cursor="Hand"
                        Click="CloseButton_Click"/>
            </Grid>

            <!-- PIN -->
            <TextBlock Grid.Row="1"
                       x:Name="PinText"
                       Text="000000"
                       FontSize="40" FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       FontFamily="Consolas"
                       Margin="0,0,0,8"/>

            <!-- Countdown progress -->
            <ProgressBar Grid.Row="2"
                         x:Name="CountdownBar"
                         Minimum="0" Maximum="100"
                         Height="4"
                         Foreground="#D0BCFF"
                         Background="#3A3A3A"
                         VerticalAlignment="Top"/>

            <!-- Remaining time -->
            <TextBlock Grid.Row="3"
                       x:Name="RemainingText"
                       Text="5:00 remaining"
                       FontSize="11"
                       Foreground="#9E9E9E"
                       HorizontalAlignment="Center"
                       Margin="0,6,0,12"/>

            <!-- Actions -->
            <Button Grid.Row="4"
                    x:Name="CopyButton"
                    Content="Copy PIN"
                    Padding="16,6"
                    HorizontalAlignment="Center"
                    Background="#6750A4" Foreground="White"
                    FontWeight="Medium"
                    Cursor="Hand"
                    Click="CopyButton_Click"/>
        </Grid>
    </Border>
</Window>
